name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: build & test (node ${{ matrix.node }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages (no DTS — проверка типов отдельным шагом)
        run: pnpm -r build
        env:
          SKIP_DTS: "true"

      - name: Run tests
        run: pnpm -r test

      - name: Check types (core only, node 22)
        if: matrix.node == 22
        working-directory: packages/core
        run: npx tsc --noEmit --allowJs --checkJs --strict false 2>&1 | head -50 || true

      - name: Check for stale dist/
        if: matrix.node == 22
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "::warning::Working tree dirty after build — dist/ might be committed accidentally"
            git status --short
          fi
