---
"@intent-driven/renderer": patch
---

DataGrid: nested dataPath ("audit.creator"), kind: "datetime" (toLocaleString), kind: "propertyPopover" (вместо JSON-snippet).

Backward-compatible: flat dataPath / column.key как раньше; новые kinds — опциональны.
